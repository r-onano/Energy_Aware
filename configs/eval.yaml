eval:
  map_iou_thresholds: [0.5]        # also support [0.5,0.55,...,0.95] later
  latency_percentiles: [50,95]
  classes_of_interest: []          # empty = all
  missed_object_iou: 0.5
  output_dir: "results/runs"

baseline:
  detector_weights: "yolov8n.pt"
  conf: 0.25
  device: 0
